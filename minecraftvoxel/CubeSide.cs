/// <summary>
/// Represents the side of a block.  The order of the values is important as they are used as lookup indices into various arrays.
/// </summary>
public enum CubeSide {
    BOTTOM,     // starts at 0
    TOP,
    LEFT,
    RIGHT,
    FRONT,
    BACK
};
